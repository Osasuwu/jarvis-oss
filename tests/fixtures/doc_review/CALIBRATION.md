# review-doc calibration (fixture for tests/test_doc_review.py)

A stand-in for `.agents/skills/review-doc/CALIBRATION.md` with a stored drift key. The key was
recorded for an older model, so any run that resolves a different model mismatches it (AC15).

drift-key: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef (model: claude-opus-4-1-20250805)
